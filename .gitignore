# Build artifacts -- the rubric asks for source only, no executables
cubeview
*.o

# Benchmark and debug output
*.ppm
*.png

# Texture packs and the atlases built from them. Third-party art must not be
# redistributed: the repository is made public for grading, so packs stay out.
*.vxtx
textures/
packs/

# Local tooling cache
.atl/
